rm -rf build bin
mkdir build && cd build
if command -v ninja &> /dev/null; then
    cmake .. -GNinja -DCMAKE_BUILD_TYPE=Release
else
    cmake .. -G"Unix Makefiles" -DCMAKE_BUILD_TYPE=Release
fi
cmake --build . -- -j$(nproc)
cd ..
echo "Build complete! Executables in bin/Release/x64/"